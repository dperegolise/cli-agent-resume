# Milestone Report: m1-scaffold

**Branch**: `m1-scaffold`  
**Date**: 2026-06-08  
**Status**: Complete — `npm run build` passes, `dist/assets/manifest.json` contains 9 entries

---

## What was built

### A. package.json + Vite project
- `package.json` with exact version pins from strategy §8:
  - `@xterm/xterm@6.0.0`, `@xterm/addon-fit@0.11.0`, `@xterm/addon-web-links@0.12.0`
  - `@codemirror/view@6.28.0`, `@codemirror/state@6.4.0`, `@codemirror/lang-markdown@6.2.0`
  - `@replit/codemirror-vim@6.3.0`
  - `vite@8.0.16`, `typescript@5.9.3`
- `vite.config.ts` — custom `generate-manifest` plugin that:
  - Scans `www/**/*.md` recursively
  - Extracts title from H1 (or YAML front-matter `title:`)
  - Strips H1 and front-matter from excerpt (first 150 chars of body)
  - Emits `dist/assets/manifest.json` with all entries
  - Copies raw `.md` files to `dist/www/` for runtime fetching
- `tsconfig.json` — strict mode, `vite/client` types, `moduleResolution: "bundler"`

### B. src/ modules
- `src/types.ts` — all shared TypeScript interfaces: `ManifestEntry`, `Manifest`, `ThemeConfig`, `ThemeColors`, `FocusFileEvent`, `ThemeChangeEvent`, `EditorSyncEvent`, `ExplorerHighlightEvent`, `SearchResultsEvent`, `SearchResult`, `ChatMessage`, `AgentRequest`, `SSETokenEvent`, `SSEFocusItemEvent`, `SSESearchResultsEvent`, `SSEDoneEvent`, `SSEErrorEvent`, `SSEEvent` union, `ToolCallMessage`, `SystemMessage`
- `src/theme.ts` — **fully implemented**: `ThemeManager` class, `GRUVBOX_DARK` / `NORD` / `TOKYO_NIGHT` theme objects with all 16 ANSI colors, `THEME_NAMES`, `applyThemeCSSVars()`, `toXtermTheme()` (converts ThemeConfig → xterm.js ITheme)
- `src/manifest.ts` — **fully implemented**: `loadManifest()` (fetch + validate + cache), `getManifestEntry()`, `getAllPaths()`, `validatePath()` (format + manifest existence check)
- `src/bus.ts` — **stub**: `EventBus` class with correct `emit()`, `subscribe()`, `once()` signatures; `EVENT_TYPES` constants; global `bus` singleton. Ready for m4 to fill in.
- `src/index.ts` — entry point: loads manifest, applies theme CSS vars, mounts all 4 panel stubs
- `src/panels/agent-shell.ts` — stub (`initAgentShell`)
- `src/panels/vim-panel.ts` — stub (`initVimPanel`)
- `src/panels/cli-drawer.ts` — stub (`initCLIDrawer`)
- `src/panels/file-explorer.ts` — stub (`initFileExplorer`)
- `src/utils/logging.ts` — `Logger` class with debug/info/warn/error levels, `createLogger(module)`, `logger` default export

### C. index.html
- Three-panel DOM skeleton with exact IDs from strategy §3:
  - `agent-shell`, `right-panel`, `file-explorer`, `divider-vertical`, `divider-horizontal`, `vim-editor-container`, `vim-editor`, `powerline-status-bar`, `cli-drawer`, `divider-bottom`
  - `hamburger-menu`, `mobile-sidebar`, `mobile-file-explorer`
- JetBrains Mono via Google Fonts CDN
- Nerd Font symbols subset via `@fontsource/symbols-nerd-font` CDN
- CSS reset (box-sizing, margin: 0, full viewport), full 3-panel grid layout definition
- CSS custom properties (`:root`): `--tmux-green: #44ff88`, `--bg-main`, `--fg-main`, all `--ansi-{0-15}` (Gruvbox Dark Hard defaults)

### D. www/ content (9 files)
Real portfolio content for Daniel Peregolise:
- `www/index.md` — name, tagline, GitHub/LinkedIn/email links
- `www/about.md` — 3-paragraph bio, tech philosophy, current stack
- `www/contact.md` — preferred contact methods, what he's interested in
- `www/experience/index.md` — career arc summary
- `www/experience/role-1.md` — Staff Engineer, Inference Platform at Helix AI (2023–present)
- `www/experience/role-2.md` — Senior Engineer, Developer Platform at Carto Systems (2020–2023)
- `www/projects/index.md` — projects overview
- `www/projects/project-1.md` — CLI Portfolio Agent (this site, meta!)
- `www/projects/project-2.md` — Kestrel (BM25 + embedding hybrid search library)

### E. .gitignore
`dist/`, `node_modules/`, `.claude/worktrees/`, `.venv/`, `venv/`, `__pycache__/`, `*.pyc`, `.env`, `.env.local`

---

## Verification

```
npm install   → 47 packages installed, exact versions per strategy §8
npm run build → tsc + vite build, success in ~100ms
```

Build output:
- `dist/assets/manifest.json` — 9 entries (about, contact, experience/{index,role-1,role-2}, index, projects/{index,project-1,project-2})
- `dist/www/**/*.md` — all 9 markdown files copied as raw text assets
- `dist/assets/main-[hash].js` — 4.85 kB main bundle
- `dist/index.html` — 6.59 kB

TypeScript: zero errors (strict mode, `noUnusedLocals`, `noUnusedParameters`).

---

## Interfaces/contracts that other milestones depend on

### Types (src/types.ts)
All interfaces are final and correct per strategy §2, §4, §5, §7. No other milestone should need to add types here (they can import what they need).

Key ones:
- `ManifestEntry`, `Manifest` — used by manifest.ts, m4 (explorer), m6 (backend search)
- `FocusFileEvent`, `ThemeChangeEvent`, etc. — bus event payloads, used by m3/m4/m5
- `SSEEvent` union — used by m3 (sseClient)
- `AgentRequest` / `ChatMessage` — used by m3, m6

### Theme (src/theme.ts)
- `ThemeManager` — m2/m3/m4/m5 should use this for theme changes
- `toXtermTheme(theme)` — m3 and m5 use this to configure xterm.js instances
- `THEME_NAMES` — m5 CLI `theme` command uses this for tab completion

### Manifest (src/manifest.ts)
- `loadManifest()` — m3 (agent), m4 (explorer), m5 (CLI) call this on startup
- `validatePath()` — m3 calls this before emitting FOCUS_FILE events
- `getAllPaths()` — m5 uses this for tab completion

### Bus (src/bus.ts)
- `bus` singleton and `EVENT_TYPES` — imported by m3, m4, m5
- **m4 owns the full implementation** — must replace stubs with real pub/sub logic

### DOM IDs (index.html)
All mount-point IDs are established. Future milestones must use these exact IDs:
- `agent-shell` (m3), `file-explorer` + `vim-editor` + `powerline-status-bar` (m4), `cli-drawer` (m5)

---

## What was NOT done (deferred to later milestones)
- Real xterm.js initialization (m3, m5)
- CodeMirror editor (m4)
- EventBus real implementation (m4)
- CSS layout/grid details beyond the base reset (m2)
- Backend Python files (m6, m7, m8)
